# Reference LaTeX importer (v5 plugin-api): wasm32, public ABI only.
[package]
name = "latex-importer"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
